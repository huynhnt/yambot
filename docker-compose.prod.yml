services:
  yambot-api:
    image: ghcr.io/huynhnt/yambot:latest
    container_name: yambot-api
    ports:
      - "8080:8080"
    environment:
      - PYTHONUNBUFFERED=1
    restart: always

networks:
  default:
    external: true
    name: shared-network
